extends layout

block content
    form(action="" method="POST")
        label(for="name") Name: 
            input(
                type="text"
                name="name"
                id="name"
            )
            label(for="email") Email: 
            input(
                type="email"
                name="email"
                id="email"
            )
            input(type="submit" value="SUBMIT")